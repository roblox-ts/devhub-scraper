Fires just after a [`ScriptDocument`](https://create.roblox.com/docs/reference/engine/classes/ScriptDocument) object is created and parented
to the service, which happens right after the script editor opens.